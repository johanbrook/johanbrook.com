---
title: Reading
layout: layouts/main.njk
description: Books I've read.
updated: 2023-05-17
wide: true
menu:
  visible: true
  order: 4
---

<main role="main">
  <article class="page">
	<h1 class="title">{{ title }}.</h1>

	<div class="flex mb5 items-end justify-between">
    	<p class="measure mb0 flex-1 muted">
    		Non-exhaustive list of books I've read and am reading, in reverse chronological order. Updated on <time class="text-color fw5" datetime="{{ updated | date("DATETIME") }}">{{ updated | date("EEEE, LLLL do yyyy") }}</time>.
		</p>

		<p class="mb0"><a href="/reading.xml">Feed{{ comp.icon({ class: "ml2" }) | safe }}</a></p>
	</div>

    <table class="textual">
    	<tr>
    		<th>Title</th>
    		<th>Author</th>
    		<th>Notes</th>
    		<th title="Status"></th>
    	</tr>
      {% for book in books | reverse %}
        <tr id="{{ book.slug }}">
        	<td>
				{{ book.title }}
        	</td>
        	<td class="f5">
        		{{ book.author }}
        	</td>
        	<td class="f5">
				{{ book.notes | md(true) | safe }}
        	</td>
        	<td class="plain-font-features no-wrap f5 fw6 {% if book.dropped %}muted{% else %}{% if book.paused %}comp-yellow{% else %}{% if not book.finished%}comp-neon{% endif %}{% endif %}{% endif %}">
            	{% if book.dropped %}
              		Dropped
              	{% else %}
              		{% if book.paused %}
	            		Paused
		            {% else %}
	              		{% if book.finished %}
	              			{% if book.finishedAt %}
	              				<time title="Finished at" datetime="{{ book.finishedAt }}">{{ book.finishedAt | date }}</time><br>
	              			{% endif %}
	              			{% if book.location %}
	              				<small title="Finished in">{{ book.location }}</small>
	              			{% endif %}
	              		{% else %}
		              		Reading
		            	{% endif %}
	            	{% endif %}
            	{% endif %}
        	</td>
        </tr>
      {% endfor %}
    </table>

    <p class="mt4">{{ books.length }} books <span class="mx1">|</span> <a href="/reading.xml">Feed{{ comp.icon({ class: "ml2" }) | safe }}</a></p>
  </article>
</main>
